import Vue from 'vue';
import {
  Button,
  Row,
  Col,
  Card,
  Progress,
  Drawer,
  Dialog,
  Form,
  FormItem,
  Input,
  Select,
  Option,
  Autocomplete,
  Table,
  TableColumn,
  Pagination,
  Collapse,
  CollapseItem,
  Loading,
  Tooltip,
  Tag,
  InfiniteScroll
} from 'element-ui';

Vue.use(Button)
  .use(Row)
  .use(Col)
  .use(Card)
  .use(Progress)
  .use(Drawer)
  .use(Dialog)
  .use(Form)
  .use(FormItem)
  .use(Table)
  .use(TableColumn)
  .use(Pagination)
  .use(Input)
  .use(Select)
  .use(Option)
  .use(Autocomplete)
  .use(Collapse)
  .use(CollapseItem)
  .use(Tooltip)
  .use(Tag)
  .use(InfiniteScroll)
  .use(Loading);
